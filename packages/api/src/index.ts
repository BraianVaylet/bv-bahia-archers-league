/**
 * Punto de entrada del backend.
 *
 * Scaffold de `INF-2`. El servidor real —Hono, middlewares de seguridad,
 * conexión a MongoDB y rutas— se construye en `BE-1` y `BE-2`.
 *
 * Ver `docs/ARCHITECTURE.md` §4 y `docs/ACTION_PLAN.md`.
 */

console.info('@bal/api — scaffold. El servidor se implementa en BE-1 y BE-2.');
