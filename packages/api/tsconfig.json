{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // NodeNext (no Bundler): el build se ejecuta con `node dist/index.js`, así que
    // los imports relativos deben llevar extensión `.js` explícita.
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": ["node"],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
